# 개발프롬프트.md — 메인 코딩 에이전트에게 그대로 전달

너는 이 프로젝트의 메인 개발 에이전트다.

학교 반 부스에서 사용할 실제 운영용 **보드게임 카페 테이블오더 + KDS + POS + 정산 시스템**을 만들어라.

프로젝트명은 **BoardBite POS**, GitHub 저장소명은 `boardbite-pos`로 한다.

중요: 바로 코딩부터 하지 마라. 이번 프로젝트는 조사 → 설계 → 구현 → 테스트 → 운영문서 순서로 진행한다. 오래 걸려도 괜찮으니 기능 누락보다 정확성과 운영 안정성을 우선한다.

---

## 1. 가장 먼저 할 일

현재 디렉터리와 Git 상태를 확인한다.

GitHub 저장소가 없다면 `gh`를 사용해 다음 조건으로 생성한다.

```bash
gh repo create boardbite-pos \
  --public \
  --description "보드게임 카페형 학교 부스를 위한 테이블오더·KDS·POS·정산 시스템" \
  --add-readme \
  --license mit \
  --clone
```

이미 repo가 있으면 중복 생성하지 않는다.

Public repository다. 절대로 `.env`, 실제 비밀번호, API key, session secret, 운영 DB를 커밋하지 마라.

`.gitignore`와 `.env.example`을 초기에 제대로 작성한다.

---

## 2. 프로젝트 문서부터 읽고 만든다

루트에 있는:

```text
AGENTS.md
요구사항.md
```

를 처음부터 끝까지 읽는다.

그리고 아래 문서를 만든다.

```text
docs/RESEARCH.md
docs/ARCHITECTURE.md
docs/SECURITY.md
docs/DEVLOG.md
docs/TEST-PLAN.md
docs/OPERATIONS.md
docs/HANDOFF.md
docs/adr/
```

처음에는 내용이 비어 있어도 되지만, 구현 전에 RESEARCH / ARCHITECTURE / SECURITY / TEST-PLAN은 충분히 작성한다.

---

## 3. Phase 0 — 무조건 조사부터 해라

코드 작성 전에 웹 검색과 공식 문서를 사용해 아래를 조사한다.

서브 에이전트를 사용할 수 있다면 적극적으로 병렬 조사시켜라.

### Agent A — OKPOS/POS 조사

다음을 최대한 실제 화면/공식 매뉴얼 기준으로 조사한다.

- OKPOS 후불형 외식업 흐름
- 테이블 선택
- 주문 누적
- 중간계산
- 결제
- 총금액
- 받을금액
- 받은금액
- 거스름돈
- 현금
- 카드
- 복합결제
- 부분결제
- 결제 후 추가주문
- QR오더
- 테이블 점유 상태
- 더치페이
- 상품별 계산과 유사한 기능
- 주문/결제 취소

우선 참고:

https://okpos.gitbook.io/okpos-guide/
https://qr.okpos.co.kr/home

단순히 "OKPOS는 이런 것 같다"라고 추측하지 말고 공식 문서/매뉴얼로 확인한다.

### Agent B — 배민/KDS 조사

배민 주문접수 PC/Lite와 KDS를 조사한다.

- 신규 주문 알림
- 주문 카드
- 접수대기
- 주문접수
- 주문거부
- 거부사유
- 조리시간
- 조리시작
- 준비완료
- 처리중
- 완료
- 취소
- 주문 이력
- 품절
- 알림음

우선 참고:

https://ceo.baemin.com/guide/13571
https://ceo.baemin.com/guide/2050

그리고 상용 KDS의 다음 기능을 추가 조사한다.

- 경과시간
- 지연 표시
- 품목별 완료
- 전체 완료
- station routing
- READY/served
- 네트워크 재연결
- 터치 UI

### Agent C — UX/UI 조사

사용자가 제공한 레퍼런스:

https://medium.com/@bunny753358/%EC%9E%90%EC%A3%BC-%EC%82%AC%EC%9A%A9%ED%95%98%EB%8A%94-ux-ui-%EB%A0%88%ED%8D%BC%EB%9F%B0%EC%8A%A4-%EC%82%AC%EC%9D%B4%ED%8A%B8-91381826ca00

위 글에 등장하는 레퍼런스 사이트와 모바일 UI 사례를 조사한다.

또한 Toss Design System과 Toss 스타일의 최신 모바일 UX를 조사한다.

https://developers-apps-in-toss.toss.im/design/components.html
https://developers-apps-in-toss.toss.im/design/consumer-ux-guide.html

목표:

- Toss 복제 금지
- Toss 브랜드/에셋 무단 사용 금지
- 대신 "정돈된 흰 배경, 명확한 위계, 큰 CTA, 짧은 문장, 쉬운 상태 피드백" 등의 원칙을 차용
- 고객 화면은 반드시 모바일 우선
- 화이트 모드 우선
- iPhone Safari에서 자연스럽게 동작
- 한 손 사용을 고려

### Agent D — Security 조사

OWASP 최신 자료와 일반적인 테이블오더 보안을 조사한다.

반드시 다음 위협을 정리한다.

- IDOR/BOLA
- 순차 테이블 번호 조작
- 다른 테이블 주문
- 공개 QR URL 공유
- CLOSED 테이블 주문
- 이전 손님의 세션 재사용
- 클라이언트 가격 조작
- 수량 조작
- 주문 더블탭
- replay
- 로그인 brute force
- 세션 탈취
- CSRF
- XSS
- SQL injection
- role escalation
- 관리자 API 직접 호출
- 결제 중복
- 동시 부분결제 race condition
- audit log tampering
- Public GitHub secret leak

### Agent E — 데이터/정산 조사

실제 POS에서 부분결제/복합결제/더치페이를 안전하게 표현하는 데이터 모델을 조사하고 제안한다.

특히:

- 하나의 주문에 여러 Payment
- 하나의 Payment가 특정 상품/수량에 할당되는 구조
- N명 균등분할
- 금액 직접분할
- 상품별분할
- 동일 상품 2개 중 1개만 결제
- 취소/void
- 환불
- 결제 기록 변경 금지 + 취소 레코드 방식
- 정수 KRW
- 동시성

---

## 4. 조사 결과 저장

모든 조사 결과를 `docs/RESEARCH.md`에 한국어로 정리한다.

각 항목:

```text
### 제목

조사일:
출처:
핵심 내용:
우리 시스템에 적용:
적용하지 않을 것:
이유:
```

출처 URL을 반드시 남겨라.

UI screenshot을 참고했다면 어떤 화면의 어떤 패턴을 참고했는지 텍스트로 기록한다.

특정 서비스를 그대로 복사하지 않는다.

---

## 5. 기술 스택을 바로 임의 선택하지 마라

조사 후 이 프로젝트에 적합한 기술 스택 후보를 비교한다.

조건:

- 고객은 모바일 웹
- 직원은 iPad/태블릿/PC
- Native 앱 설치 필요 없음
- 실시간 주문 상태 갱신
- 결제 데이터의 트랜잭션 일관성
- 학교 부스에서 쉽게 배포/복구
- 복잡도가 너무 높지 않을 것
- 백업이 쉬울 것
- 테스트가 쉬울 것
- Public repo로 관리
- Linux 서버 배포 가능
- HTTPS 운영 가능

후보를 비교한 뒤 `docs/adr/0001-tech-stack.md`에 선택 이유를 기록하고 나서 구현한다.

실시간 통신 방식도 WebSocket/SSE/short polling 등을 비교해 ADR로 남긴다.

---

## 6. 핵심 운영 흐름

다음 흐름이 프로젝트의 절대 기준이다.

```text
손님 입장
↓
FRONT가 빈자리 배정
↓
FRONT가 테이블 OPEN
↓
테이블에 붙은 NFC/QR 사용 가능
↓
고객 모바일 주문
↓
POS/KDS 신규 주문
↓
주방 접수
↓
조리 시작
↓
조리 완료
↓
SERVING 화면 READY
↓
서빙 완료
↓
필요시 고객 추가 주문
↓
손님 퇴장 전 FRONT 정산
↓
부분/복합/더치/상품별 결제 가능
↓
잔액 0
↓
고객 주문 세션 즉시 폐기
↓
테이블 자동 CLOSE
↓
다음 손님 배정 가능
```

테이블 OPEN을 직원이 하지 않았다면 QR/NFC를 찍어도 주문할 수 없어야 한다.

---

## 7. NFC/QR 보안

테이블 URL은 숫자 ID를 직접 쓰지 않는다.

금지:

```text
/table/1
/table/2
?table=7
```

충분히 긴 랜덤 public token을 사용한다.

예:

```text
/t/1nD9wV5k6A...
```

그렇다고 token만 믿지 않는다.

서버에서 매 요청마다:

```text
token valid?
table enabled?
table OPEN?
active table session?
customer session active?
```

를 검사한다.

테이블 CLOSE 시 기존 고객 세션은 즉시 무효화한다.

관리자는 테이블 토큰을 회전할 수 있어야 한다.

---

## 8. 인증/계정

회원가입 없음.

`.env.example`:

```env
ADMINID=admin
ADMINPASSWORD=change-me

FRONTID=front
FRONTPW=change-me

POSID=pos
POSPW=change-me

SERVING_ID=serving
SERVING_PW=change-me

SESSION_SECRET=change-me
```

실제 `.env`는 Git에 들어가면 안 된다.

고객 페이지 하단에 조용한 `관리자 로그인` 링크를 둔다.

해당 로그인 페이지는 모든 직원 계정의 공통 로그인 화면으로 사용해도 된다.

로그인 후 서버가 역할을 판정해 자동 이동:

```text
ADMIN → /admin
FRONT → /front
POS → /pos
SERVING → /serving
```

클라이언트 role selector로 권한을 결정하지 않는다.

ADMIN은 추가 직원 계정을 만들고 역할을 부여할 수 있게 한다.

환경변수 계정은 bootstrap 계정으로 취급하는 안전한 방식을 설계한다.

---

## 9. 고객 화면

무조건 모바일-first.

화이트 UI.

첫 화면 최상단에 테이블 번호를 크게 표시.

메뉴:

- 카테고리
- 이미지
- 이름
- 가격
- 설명
- 옵션
- 품절
- 수량

장바구니는 하단 sticky CTA.

주문 전 최종 확인.

주문 후:

```text
주문 확인 중
접수
조리중
준비완료
서빙완료
```

현재 세션의 누적 장부와 미수금 표시.

브라우저 새로고침 후에도 현재 상태 복구.

CLOSED 테이블에서는 메뉴 대신:

```text
현재 주문 가능한 테이블이 아닙니다.
입구에서 자리 배정을 먼저 받아주세요.
```

---

## 10. KDS/POS

배민 주문접수와 식당 KDS처럼 설계한다.

상태별 탭:

```text
신규
접수
조리중
준비완료
이력
취소
```

카드:

- 주문번호
- 테이블
- 주문시각
- 경과시간
- 메뉴/수량
- 옵션
- 요청사항

동작:

- 접수
- 거부 + 이유
- 조리 시작
- 품목별 완료
- 전체 완료
- 취소 + 이유

새 주문은 알림음.

오래된 주문은 단계적으로 강조.

색만으로 구분하지 않는다.

기존 주문 이력과 취소 주문을 검색 가능하게 한다.

---

## 11. SERVING

READY 주문만 우선 표시.

```text
7번 테이블
#A-031
짜장우동 ×2
콜라 ×1
준비완료 후 01:20
[서빙 완료]
```

오래 READY인 주문 강조.

고객의 직원 호출 기능을 구현한다면 여기서 확인/완료 처리.

---

## 12. FRONT/POS 정산

OKPOS의 정보 구조는 참고하되 현대적인 UI로 설계한다.

반드시 보여줄 값:

```text
총금액
할인금액
결제금액
남은금액
받은금액
거스름돈
```

결제수단:

- 현금
- 카드(기록)
- 기타

실제 카드 번호, 유효기간, CVC는 절대로 받거나 저장하지 않는다.

### 현금

받은금액을 입력하면 거스름돈을 즉시 계산한다.

### 부분결제

예:

```text
총 30,000
카드 10,000
현금 5,000
남음 15,000
```

### 복합결제

여러 수단 합산.

### 더치페이

N명을 입력하면 정확한 KRW 정수로 나눈다.

### 상품별 결제

주문 항목과 수량을 직접 선택.

동일 상품 2개 중 1개만 선택 가능.

이미 결제된 상품/수량은 다시 결제할 수 없음.

### 결제 취소

취소 사유/작업자/시각/원결제 관계를 보존.

### 완납

서버 계산 잔액이 0이면 자동으로 고객 주문 세션을 폐기하고 테이블 CLOSE.

동시 결제 race condition을 반드시 테스트한다.

---

## 13. ADMIN PAGE

별도 `/admin`.

인증 없으면 접근 불가.

기능:

- 사용자 생성/삭제/비활성
- 역할 변경
- 비밀번호 초기화
- 테이블 생성/수정/비활성
- 테이블 토큰 회전
- QR 확인/출력
- 메뉴 CRUD
- 가격 변경
- 품절
- 주문 검색
- 주문 상태 강제 정정
- 결제 검색
- 결제 기능 전체 비활성/활성
- 특정 테이블 결제 잠금
- 모든 감사로그 조회
- 로그 필터
- 로그 export
- 로그 삭제/정리
- 매출
- 결제수단별 매출
- 메뉴 판매량
- 운영 설정
- DB 백업/내보내기

관리자 액션은 감사 로그를 남긴다.

로그 삭제 동작도 최소한의 별도 감사 흔적을 남겨라.

---

## 14. 보드게임 이용시간

요금제:

```text
10분
20분
30분
```

등을 ADMIN에서 자유롭게 설정.

테이블 OPEN 시 FRONT가 선택.

해당 이용료를 테이블 장부에 추가.

서버 시각으로 만료 계산.

고객/FRONT/SERVING에서 남은 시간 확인.

시간 연장 가능.

종료 임박/초과 시 직원 화면에서 명확한 경고.

---

## 15. 디자인

이 프로젝트는 다크모드 중심 앱이 아니다.

기본은 WHITE.

고객 모바일:

- 360~430px 우선
- iPhone safe-area
- 하단 CTA
- 44px 이상 터치영역
- 큰 금액
- 큰 테이블 번호
- 짧은 문장
- 한 화면 한 가지 핵심 행동
- 정돈된 spacing
- skeleton/loading
- 명확한 error
- 명확한 sold-out
- 적당한 radius
- 과한 shadow 금지
- 과한 gradient 금지

Toss와 최신 한국 모바일 서비스의 "정보를 빠르게 이해시키는 방식"을 참고하되 복제하지 않는다.

직원 화면은 기능성이 우선이며, KDS는 멀리서도 읽혀야 한다.

---

## 16. 보안은 마지막에 붙이지 마라

구현 단계마다 Security Agent에게 리뷰시켜라.

반드시 테스트:

- 다른 테이블 token/ID 접근
- CLOSED 테이블 주문
- 이전 고객 session 재사용
- client price tampering
- role escalation
- admin API direct access
- CSRF
- XSS
- injection
- brute force
- 주문 replay
- 주문 double submit
- 동시 부분결제
- 초과결제
- 결제 취소 권한
- Public repo secret scan

---

## 17. 개발일지

`docs/DEVLOG.md`는 무조건 계속 갱신한다.

각 작업 끝에:

```markdown
## YYYY-MM-DD HH:MM

### 작업자
### 목적
### 조사
### 변경 파일
### 구현 내용
### 설계 결정
### 테스트
### 알려진 문제
### 다음 작업
### 커밋
```

를 남긴다.

다른 에이전트가 갑자기 투입되어도 DEVLOG만 읽고 이어갈 수 있을 정도로 구체적으로 쓴다.

---

## 18. 커밋

큰 덩어리 하나로 커밋하지 않는다.

예:

```text
docs: add POS and KDS research
docs: define security threat model
feat: implement staff authentication and RBAC
feat: implement table session lifecycle
feat: add customer ordering flow
feat: add kitchen display workflow
feat: add serving dashboard
feat: add split payment ledger
feat: add item-based split checkout
feat: add admin audit log viewer
test: add concurrent payment tests
```

작업 중간중간 실행 가능한 상태를 유지한다.

---

## 19. 테스트

Unit + integration + E2E를 적절히 사용한다.

최소 E2E:

```text
FRONT 로그인
→ 테이블 OPEN
→ 고객 주문
→ POS 접수
→ 조리 시작
→ 조리 완료
→ SERVING 완료
→ FRONT 부분결제
→ 상품별 결제
→ 잔액 0
→ 자동 CLOSE
→ 이전 고객 재주문 차단
```

동시성 테스트:

- 두 주문 동시 제출
- 같은 주문 중복 제출
- 두 FRONT가 동시 결제
- 같은 상품 수량을 동시에 부분결제

---

## 20. 운영 준비

최종적으로 `docs/OPERATIONS.md`에 실제 부스 직원용 사용법을 작성한다.

내용:

- 서버 시작
- 서버 종료
- 직원 로그인
- 자리 배정
- 테이블 OPEN
- NFC/QR 문제 대응
- 주문 누락 대응
- 주방 화면
- 서빙
- 현금 정산
- 더치페이
- 상품별 결제
- 잘못 결제했을 때
- 테이블 강제 CLOSE
- 품절 처리
- 네트워크 장애
- 백업
- 마감
- 다음날 초기화

개발자가 아닌 학생도 보고 사용할 수 있는 문장으로 쓴다.

---

## 21. 마지막 검수

완료라고 말하기 전에 직접 다음을 확인한다.

- README만 보고 신규 환경 실행 가능
- Public GitHub에 secret 없음
- 모바일 고객 화면 깨짐 없음
- iPhone Safari 테스트
- Android Chrome 테스트
- iPad/태블릿 FRONT 테스트
- POS/KDS 장시간 표시 테스트
- 여러 테이블 동시 주문
- 여러 기기 같은 테이블 주문
- 재연결
- 중복 요청
- 부분결제
- 복합결제
- 더치페이
- 상품별 결제
- 결제 취소
- 자동 CLOSE
- CLOSED 재주문 차단
- 역할별 403 확인
- 감사 로그
- 백업/복구
- `docs/HANDOFF.md` 최신화

기능을 구현하지 못한 것이 있다면 숨기지 말고 정확히 적어라.

대충 데모만 되는 수준으로 끝내지 말고, 실제 하루 부스 운영 중 계속 켜 놓고 써도 되는 것을 목표로 한다.
