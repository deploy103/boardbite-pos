# AGENTS.md — BoardBite POS 개발 에이전트 운영 규칙

## 0. 프로젝트 정체성

프로젝트명: **BoardBite POS**
저장소명: `boardbite-pos`

학교 반 부스의 보드게임 카페 운영을 위한 웹 기반 주문·테이블·주방(KDS)·서빙·정산·관리 시스템이다.

핵심 운영 방식은 다음과 같다.

1. 손님은 입구/프론트에서 자리를 배정받는다.
2. FRONT 직원이 해당 테이블을 `OPEN` 한다.
3. 손님은 테이블에 붙은 NFC 또는 QR을 이용해 자신의 테이블 주문 페이지에 들어간다.
4. 테이블이 OPEN 상태일 때만 주문할 수 있다.
5. 주문은 주방(POS/KDS)에 실시간으로 표시된다.
6. 주방은 주문을 접수 → 조리 시작 → 조리 완료로 처리한다.
7. SERVING 직원은 조리 완료 주문을 확인하고 테이블에 전달한 후 서빙 완료 처리한다.
8. 손님이 나갈 때 FRONT에서 해당 테이블을 정산한다.
9. 잔액이 0원이 되면 고객 주문 세션을 즉시 폐기하고 테이블을 자동 CLOSE 한다.
10. 결제/정산 완료 후 이전 고객의 브라우저 세션으로는 새 주문을 할 수 없어야 한다.

이 프로젝트는 실제 카드 VAN/PG를 직접 처리하는 결제 시스템이 아니다. 카드 결제를 연동하지 않는 한, 카드/현금/기타 결제는 "매장에서 실제로 받은 결제를 장부에 기록"하는 기능으로 취급한다.

---

## 1. 가장 중요한 원칙

### 1.1 먼저 조사하고, 그 다음 개발한다

코드를 바로 작성하지 마라.

최초 개발 시작 전에 반드시 다음을 조사하고 결과를 `docs/RESEARCH.md`에 한국어로 기록한다.

- 실제 국내 외식업 POS의 테이블/후불 주문 흐름
- OKPOS의 후불형 테이블 운영, 받은금액/받을금액/거스름돈, 복합결제, 부분결제, QR오더
- 배민 주문접수 PC/Lite의 주문접수, 거부, 조리시간, 조리시작, 준비완료, 주문이력 UX
- KDS(Kitchen Display System)의 신규 주문 알림, 경과 시간, 지연 표시, 주문/품목 단위 완료 방식
- QR/NFC 테이블 오더의 보안 방식
- 모바일 주문 UI/UX 사례
- Toss Design System 및 최신 모바일 금융/커머스 UI 패턴
- OWASP 권한검사, IDOR/BOLA, CSRF, XSS, Rate Limit, 세션 보안
- iOS Safari/Android Chrome 모바일 웹 호환성

조사한 내용에는 반드시 다음을 남긴다.

- 조사일
- 출처 URL
- 출처가 말하는 핵심
- 우리 프로젝트에 적용할 내용
- 적용하지 않을 내용과 이유
- 출처를 그대로 복제하지 않고 어떤 원칙만 참고했는지

### 1.2 에이전트가 바뀌어도 이어서 개발할 수 있어야 한다

작업 시작 전에 반드시 아래를 읽는다.

1. `AGENTS.md`
2. `요구사항.md`
3. `docs/RESEARCH.md`
4. `docs/ARCHITECTURE.md`
5. `docs/SECURITY.md`
6. `docs/DEVLOG.md` 마지막 기록
7. `git status`
8. 최근 커밋 로그

작업 종료 전에는 반드시 `docs/DEVLOG.md`를 갱신한다.

DEVLOG에는 최소 다음을 기록한다.

- 날짜/시간
- 작업자 또는 에이전트 식별자
- 이번 작업 목적
- 조사한 내용
- 수정한 파일
- 구현 내용
- 설계 결정
- 실행한 테스트와 결과
- 발견된 버그
- 아직 해결하지 않은 문제
- 다음 에이전트가 가장 먼저 해야 할 일
- 관련 커밋 해시

"코드는 바뀌었는데 DEVLOG는 안 바뀌는 상황"을 금지한다.

### 1.3 불확실한 결정은 ADR로 남긴다

중요한 설계 선택은 `docs/adr/NNNN-제목.md`에 ADR(Architecture Decision Record)로 기록한다.

예:

- 인증/세션 방식
- 실시간 업데이트 방식
- 데이터베이스 선택
- 주문 상태 머신
- 결제/부분결제 데이터 모델
- 테이블 공개 토큰 방식
- 배포 방식
- 로그 보관/삭제 정책

---

## 2. GitHub 저장소 운영

저장소가 없다면 GitHub CLI `gh`로 다음 조건의 저장소를 만든다.

- 이름: `boardbite-pos`
- 공개(Public)
- README 포함
- MIT License 포함
- 설명: `보드게임 카페형 학교 부스를 위한 테이블오더·KDS·POS·정산 시스템`
- 기본 브랜치: `main`

예시:

```bash
gh repo create boardbite-pos \
  --public \
  --description "보드게임 카페형 학교 부스를 위한 테이블오더·KDS·POS·정산 시스템" \
  --add-readme \
  --license mit \
  --clone
```

이미 저장소가 있다면 새로 만들지 말고 기존 저장소를 사용한다.

### 2.1 절대 커밋하면 안 되는 것

- `.env`
- 실제 비밀번호
- 실제 세션 키
- 실제 DB 암호
- 실제 운영 토큰
- 개인 정보
- 운영 백업 DB
- 결제 정보

반드시 `.env.example`만 커밋한다.

Public repository라는 점을 항상 전제로 한다.

---

## 3. 문서 구조

최소 다음 문서를 유지한다.

```text
README.md
AGENTS.md
요구사항.md
.env.example

docs/
  RESEARCH.md
  ARCHITECTURE.md
  SECURITY.md
  DEVLOG.md
  TEST-PLAN.md
  OPERATIONS.md
  HANDOFF.md
  adr/
```

README는 프로젝트 소개만 쓰는 장식 문서가 아니다. 신규 개발자가 README 하나로 로컬 실행, 환경변수, 기본 계정 구성, 테스트, 배포 개요까지 파악할 수 있게 한다.

---

## 4. 서브 에이전트 사용 원칙

가능한 환경이면 서브 에이전트를 적극 사용한다.

초기 단계에서 최소 다음 역할로 분리해 병렬 조사한다.

### Research Agent
실제 POS, OKPOS, QR오더, 배민 주문접수, KDS 사례를 조사한다.

### UX Agent
사용자가 제공한 UX/UI 레퍼런스와 Toss Design System, 모바일 주문 사례를 조사하고 모바일 화면 흐름을 설계한다.

### Security Agent
테이블 접근, 세션, RBAC, IDOR/BOLA, 주문 위변조, 중복결제, CSRF/XSS, 로그, 비밀정보 관리 등을 위협 모델링한다.

### Data/Payment Agent
주문/주문항목/결제/분할결제/상품별 결제/환불/테이블 세션 모델과 상태 머신을 설계한다.

### QA Agent
요구사항 기반 테스트 케이스, 동시성 테스트, 권한 우회 테스트, 모바일 테스트를 작성한다.

각 서브 에이전트 결과는 메인 에이전트가 검토하고 문서에 병합한다. 서로 같은 파일을 무작정 동시에 수정하게 하지 말고, 조사 결과를 개별 초안으로 받은 뒤 메인 에이전트가 정리한다.

---

## 5. 구현 순서

기능을 한꺼번에 만들지 말고 아래 순서로 진행한다.

### Phase 0 — 조사
`docs/RESEARCH.md` 작성. 코드 작성 금지.

### Phase 1 — 설계
다음을 작성한다.

- 화면/사용자 흐름
- 역할 권한표
- 데이터 모델
- 상태 머신
- 보안 위협 모델
- API 계약
- 오류/복구 정책
- 테스트 계획
- 기술 스택 비교 및 선택 ADR

### Phase 2 — 핵심 기반
- 인증
- 역할 권한
- 메뉴
- 테이블
- 테이블 OPEN/CLOSE
- 고객 테이블 세션
- 주문 생성
- 감사 로그

### Phase 3 — 주방/KDS
- 신규 주문
- 접수/거부
- 조리 시작
- 조리 완료
- 취소/이력
- 경과 시간
- 신규 주문 알림

### Phase 4 — SERVING
- READY 목록
- 테이블별 서빙
- 서빙 완료
- 지연 주문/누락 확인

### Phase 5 — FRONT/POS 정산
- 테이블 장부
- 총액
- 받은금액
- 거스름돈
- 결제수단
- 부분결제
- 복합결제
- N명 더치페이
- 상품별 분할결제
- 결제 취소/정정
- 잔액 0원 자동 CLOSE

### Phase 6 — ADMIN
- 사용자/역할
- 메뉴/품절
- 테이블 관리
- 결제 기능 잠금
- 로그 조회/정리
- 매출/운영현황
- 설정

### Phase 7 — 안정화
- 모바일 실기기 테스트
- 동시 주문
- 중복 요청
- 동시 결제
- 권한 우회
- 네트워크 끊김/재연결
- 백업/복구
- 부하/장시간 운영
- 운영 매뉴얼

각 Phase 종료 시 테스트 통과 후 커밋하고 DEVLOG에 남긴다.

---

## 6. 보안 규칙

### 6.1 고객에게 내부 테이블 ID를 신뢰시키지 않는다

다음과 같은 공개 URL 설계를 금지한다.

```text
/order?table=1
/order?table=2
/table/7
```

공개 NFC/QR 주소에는 추측하기 어려운 랜덤 토큰을 사용한다.

예:

```text
/t/2_x4B0M7dbyfWm8P...
```

서버만 이 토큰과 실제 테이블을 매핑한다.

랜덤 토큰을 사용한다고 권한검사를 생략하면 안 된다.

모든 고객 요청에서 서버가 다음을 재검증한다.

- 토큰이 존재하는가
- 테이블이 활성화 상태인가
- 현재 OPEN 상태인가
- 현재 테이블 세션과 연결 가능한가
- 세션이 만료/폐기되지 않았는가

### 6.2 테이블 OPEN 상태에서만 고객 주문을 허용한다

CLOSED 테이블 NFC/QR 접근 시:

```text
현재 주문 가능한 테이블이 아닙니다.
입구에서 자리 배정을 먼저 받아주세요.
```

를 보여준다.

결제가 끝나 테이블이 CLOSE되면 기존 고객 세션을 즉시 무효화한다.

### 6.3 고객은 가격을 결정하지 않는다

클라이언트가 아래 값을 보내더라도 신뢰하지 않는다.

- 가격
- 총액
- 할인
- 테이블 번호
- 주문 상태
- 결제 여부

고객은 메뉴 식별자, 수량, 허용된 옵션 정도만 요청한다.

가격/옵션 가격/합계는 서버 데이터로 다시 계산한다.

금액은 KRW 정수로 저장한다. 부동소수점 금지.

### 6.4 역할과 권한은 서버에서 검사한다

역할:

- CUSTOMER
- FRONT
- POS
- SERVING
- ADMIN

화면을 숨기는 것만으로 권한을 통제하지 않는다.

API마다 서버 측 권한검사를 한다.

### 6.5 STAFF 인증

환경변수:

```env
ADMINID=
ADMINPASSWORD=
FRONTID=
FRONTPW=
POSID=
POSPW=
SERVING_ID=
SERVING_PW=
SESSION_SECRET=
```

비밀번호를 로그에 출력하지 않는다.

`.env`는 절대 Git에 커밋하지 않는다.

환경변수 계정은 최초/부트스트랩 운영 계정으로 취급한다. 추가 직원 계정은 ADMIN이 생성할 수 있다.

저장 비밀번호는 안전한 비밀번호 해시 알고리즘을 사용한다.

세션 쿠키는 운영 환경에서 최소 다음 속성을 적용한다.

- HttpOnly
- Secure
- SameSite 적절한 값
- 적절한 만료
- 로그인/권한 변경 시 세션 재발급

### 6.6 로그인 공격 방어

- 로그인 실패 횟수 제한
- IP/계정 단위 rate limit
- 반복 실패 시 일시 잠금
- 로그인 성공/실패 감사 로그
- 기본 비밀번호 운영 전 변경 경고

### 6.7 주문 중복 방지

모바일 네트워크 재시도나 더블탭 때문에 같은 주문이 두 번 들어가지 않도록 주문 생성 요청에 idempotency 처리를 적용한다.

### 6.8 결제 동시성

부분결제/상품별 결제/복합결제 중 두 직원이 동시에 정산해도 초과 결제되지 않아야 한다.

결제 반영은 트랜잭션과 동시성 제어를 사용한다.

서버는 항상 현재 미수금액을 다시 계산하고 검증한다.

### 6.9 XSS/CSRF/Injection

- 사용자 메모/요청사항 출력 시 escaping
- SQL 파라미터 바인딩 또는 안전한 ORM
- 상태 변경 API에 CSRF 대응
- Content Security Policy 검토
- 보안 헤더 적용
- 입력 길이 제한
- 허용 문자/형식 검증

### 6.10 로그

감사 로그에는 다음을 기록한다.

- 로그인 성공/실패
- 테이블 OPEN/CLOSE
- 주문 생성
- 주문 거부/취소
- 조리 상태 변경
- 서빙 완료
- 결제/부분결제/취소
- 메뉴 가격 변경
- 품절 변경
- 사용자 생성/삭제/권한 변경
- 테이블 비활성화
- 로그 삭제 작업
- 관리자 설정 변경

민감정보와 비밀번호는 절대 로그에 남기지 않는다.

관리자가 로그를 정리할 수 있어야 하지만, 로그 삭제 동작 자체는 별도의 최소 감사 흔적을 남기는 방식을 설계한다.

---

## 7. UI/UX 원칙

고객 사용 환경은 **대부분 휴대폰**이다.

고객 화면은 모바일 우선으로 설계한다.

기준:

- 360px~430px 폭을 우선 설계
- iPhone Safari safe-area 고려
- 한 화면에 너무 많은 정보를 넣지 않음
- 터치 영역 최소 약 44px 이상
- 주요 CTA는 화면 하단 고정 또는 엄지 접근 영역
- 가격/수량/테이블 번호를 매우 명확하게
- 주문 확정 전 최종 확인
- 주문 성공 후 중복 제출 방지
- 긴 설명보다 짧고 명확한 한국어
- 로딩/오류/품절 상태를 명확히
- 다크모드를 기본 요구사항으로 만들지 않는다.
- **화이트 모드 우선**
- 충분한 여백
- 과도한 그림자/그라데이션/네온 금지
- 디자인은 Toss처럼 정돈되고 빠르게 이해되는 방향을 참고하되, Toss 브랜드/에셋/화면을 복제하지 않는다.

직원용 FRONT/POS/SERVING/ADMIN은 태블릿·PC에서도 효율적으로 쓸 수 있어야 한다.

KDS는 한눈에 많은 주문을 봐야 하므로 고객 화면과 다른 정보밀도를 허용한다.

---

## 8. 기능을 임의로 빼지 않는다

요구사항이 어렵다는 이유로 기능을 조용히 삭제하지 않는다.

어려운 기능은 다음 중 하나로 처리한다.

1. 구현
2. 작은 단위로 나눠 단계적으로 구현
3. 명확한 blocker와 대안을 DEVLOG에 기록
4. 사용자 승인 없이 요구사항 자체를 제거하지 않음

---

## 9. 테스트 원칙

중요 비즈니스 로직은 UI 수동 테스트만으로 끝내지 않는다.

필수 자동 테스트 대상:

- 권한검사
- 테이블 OPEN/CLOSE
- CLOSED 테이블 주문 차단
- 가격 위변조 무시
- 주문 중복 방지
- 주문 상태 전이
- 미수금 계산
- 현금 거스름돈
- 분할결제
- 상품별 결제
- 동일 상품 수량 일부 결제
- 복합결제
- 초과결제 차단
- 결제 취소/환불
- 자동 CLOSE
- 이전 고객 세션 폐기
- 사용자 역할 변경
- 동시 결제
- 로그 생성

브라우저 E2E에서는 최소 다음 흐름을 자동화한다.

```text
FRONT 로그인
→ 테이블 OPEN
→ 고객 QR URL 접근
→ 주문
→ POS 접수
→ 조리 시작
→ 조리 완료
→ SERVING 서빙완료
→ FRONT 정산
→ 자동 CLOSE
→ 기존 고객이 재주문 시 차단
```

---

## 10. 완료 조건

"화면이 뜬다"는 완료가 아니다.

최종 완료 전 다음을 검증한다.

- 새 환경에서 README만 보고 실행 가능
- `.env.example` 완성
- Public repo에 secret 없음
- 모든 역할별 실제 흐름 확인
- iPhone/Android 모바일 화면 확인
- iPad/태블릿 FRONT 화면 확인
- KDS 화면 확인
- 동시에 여러 테이블 주문 테스트
- 중복 주문 방지 테스트
- 분할결제/복합결제 테스트
- 테이블 자동 CLOSE 확인
- 권한 우회 테스트
- 백업/복구 절차 확인
- 운영자가 보는 `docs/OPERATIONS.md` 완성
- 다음 에이전트가 이어받는 `docs/HANDOFF.md` 완성
